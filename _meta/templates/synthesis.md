---
title: "{{title}}"
category: synthesis
tags: []
sources: []
summary: One sentence — the insight that only exists by combining pages.
lifecycle: draft
lifecycle_changed: "{{date}}"
tier: core
base_confidence: 0.5
provenance: {extracted: 0.3, inferred: 0.6, ambiguous: 0.1}
relationships:
  - target: "[[reference]]"
    type: derived_from
created: "{{date}}"
updated: "{{date}}"
---

# {{title}}

An insight no single source states — it exists only in the combination.

## The insight

State it plainly. Most of a synthesis page is `^[inferred]` by nature; the provenance split above should reflect that honestly.

## Pages combined

Wikilink at least two pages this synthesis is built from, and say what each contributes.

## Conflicts surfaced

Where the combined pages disagree. Flag contradictions; never silently resolve them.

## Placeholder notes (delete on use)

The example relationship above points at the sibling reference template so lint resolves — replace with the actual pages combined. Keep `lifecycle: draft`.
