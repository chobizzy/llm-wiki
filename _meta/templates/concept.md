---
title: "{{title}}"
category: concepts
tags: []
sources: []
summary: One sentence — the idea this page asserts and why it matters.
lifecycle: draft
lifecycle_changed: "{{date}}"
tier: supporting
base_confidence: 0.5
provenance: {extracted: 0.0, inferred: 0.0, ambiguous: 0.0}
relationships:
  - target: "[[entity]]"
    type: related_to
created: "{{date}}"
updated: "{{date}}"
---

# {{title}}

State the idea in one or two sentences an agent could quote verbatim.

## Why it matters

What breaks or improves when this idea is applied.

## Evidence

Paraphrase the real sources listed in `sources`. Mark your own conclusions `^[inferred]`; mark contested claims `^[ambiguous]`. Unmarked sentences must trace to a source.

## Placeholder notes (delete on use)

The example relationship above points at the sibling entity template so lint resolves — replace it with links to real pages (or remove the block). Pick 1-5 tags from the taxonomy. Set `tier` (core/supporting/peripheral) and `base_confidence` honestly. Keep `lifecycle: draft` — promotion to reviewed/verified is human-only.
